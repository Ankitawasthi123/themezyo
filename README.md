# Templify - Next.js App

An AI-based ideas marketplace built with the Next.js App Router and Tailwind CSS.

Quick start (PowerShell / CMD):

```powershell
cd templify
npm install
npm run dev
```

Notes:
- `package.json` uses Next.js, React, and React DOM.
- Run `npm install` before `npm run dev` if dependencies are missing.
